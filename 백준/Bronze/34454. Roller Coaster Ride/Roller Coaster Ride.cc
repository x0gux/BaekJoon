// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    if( a <= b*c) printf("yes");
    else printf("no");

    return 0;
}